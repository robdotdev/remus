{
  "name": "remus",
  "version": "1.0.0",
  "description": "remus webpage",
  "main": "index.js",
  "scripts": {
    "watch:sass": "sass sass/main.scss css/style.css -w",
    "devserver": "live-server",
    "start": "npm-run-all --parallel devserver watch:sass"
  },
  "author": "robdotdev",
  "license": "MIT",
  "dependencies": {
    "node-sass": "^6.0.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
